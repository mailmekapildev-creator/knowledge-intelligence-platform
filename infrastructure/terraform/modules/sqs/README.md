# Module: sqs

Placeholder module boundary. In a full implementation this directory contains
main.tf/variables.tf/outputs.tf for the sqs resource group, kept as a separate module
so it can be planned/applied/tested independently and reused across environments
(dev/staging/production) with different variable values.
